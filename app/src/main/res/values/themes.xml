<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base de la app -->
    <style name="Base.Theme.Yaku_vma" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/btn_principal</item>
        <item name="colorOnPrimary">@color/blanco</item>
        <item name="colorSecondary">@color/principal</item>
    </style>

    <!-- Tema que se aplica en AndroidManifest -->
    <style name="Theme.Yaku_vma" parent="Base.Theme.Yaku_vma" />
</resources>
